/** Shared visual constants for the tuner UI — the single place the accent lives. */
export const ACCENT = '#00E0B8';

/** ACCENT at 18% opacity — the selection-highlight fill. */
export const ACCENT_FILL = 'rgba(0, 224, 184, 0.18)';
