/**
 * Design tuner — dev-only in-app editor (docs/tuner/TODO.md).
 *
 * Entry point: wrap the root component with `withTuner`. It is a no-op in
 * production builds.
 */
export { withTuner } from './TunerRoot';
